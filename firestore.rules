
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{projectId} {
      // Allow anyone to read property listings
      allow read: if true;
      // Allow anyone to write property listings for now 
      // (This will be refined as we implement more robust authentication)
      allow write: if true;
    }
  }
}
